<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>GAMEPICK | 게임픽</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        body { background: #050505; color: #fff; font-family: 'Pretendard', sans-serif; }
        .glass-input { background: rgba(255,255,255,0.05); border: 1px solid rgba(255,255,255,0.1); backdrop-filter: blur(10px); }
        .card { background: #111; border: 1px solid #222; padding: 30px; border-radius: 24px; transition: 0.3s; }
        .card:hover { border-color: #444; transform: translateY(-5px); }
        .tag { background: #ff2d55; color: #white; padding: 4px 10px; border-radius: 8px; font-weight: 800; font-size: 12px; }
        .score-steam { color: #66c0f4; font-weight: bold; font-size: 12px; }
        .score-meta { background: #6c3; color: #000; padding: 2px 6px; border-radius: 4px; font-weight: 900; font-size: 11px; display: inline-block; margin-top: 4px; }
        .btn-link { background: #fff; color: #000; padding: 8px 16px; border-radius: 12px; font-weight: bold; font-size: 13px; }
    </style>
</head>
<body class="max-w-xl mx-auto px-6 py-20">

    <header class="text-center mb-16">
        <h1 class="text-4xl font-black tracking-tighter mb-2">GAME<span class="text-gray-500">PICK</span></h1>
        <p class="text-gray-500 text-xs font-bold tracking-widest uppercase">Select your next play</p>
    </header>

    <div class="flex gap-2 mb-12">
        <input type="text" id="q" placeholder="게임 이름을 입력하세요" class="flex-1 glass-input rounded-2xl px-6 py-4 outline-none focus:border-white/30 transition">
        <button onclick="pick()" class="bg-white text-black font-black px-6 rounded-2xl hover:bg-gray-200 transition">PICK</button>
    </div>

    <div id="loader" class="hidden text-center py-10 animate-pulse text-gray-500 font-bold italic">ANALYZING...</div>
    <div id="out"></div>

    <script>
        async function pick() {
            const q = document.getElementById('q').value;
            if(!q) return;
            document.getElementById('out').innerHTML = "";
            document.getElementById('loader').classList.remove('hidden');

            try {
                // Render 배포 후 받은 주소로 꼭 바꾸세요!
                const res = await fetch('https://내-서버-주소.onrender.com/search', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({ query: q })
                });
                const data = await res.json();
                document.getElementById('out').innerHTML = data.result;
            } catch (e) {
                alert("서버 연결 실패!");
            } finally {
                document.getElementById('loader').classList.add('hidden');
            }
        }
    </script>
</body>
</html>